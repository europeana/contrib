package eu.europeana.solr;

import org.junit.Test;

public class SolrFieldsTest {

	@Test
	public void test() {
		// StringBuilder map = new StringBuilder();
		// for (SolrFields field : SolrFields.values()){
		// map.append(field.toString());
		// map.append("=0,");
		// }
		//
		// Assert.assertEquals(5, SolrFields.toArray().length);
		// Assert.assertEquals(5,SolrFields.getSortedValues(map.substring(0,map.length()-1)).length);
		// Assert.assertEquals(0f,SolrFields.getSortedValues(map.substring(0,map.length()-1))[0]);
	}
}
